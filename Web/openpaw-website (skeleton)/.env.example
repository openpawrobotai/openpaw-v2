# Copy to .env.local (gitignored). Names only — never commit real values.

# Waitlist: any endpoint that accepts { email, source, ts } JSON.
# e.g. Loops, ConvertKit, a Zapier/Make webhook, Google Apps Script, or a
# Firebase function. Leave blank in dev to log leads to the console.
WAITLIST_WEBHOOK_URL=
